
package ${basePackage}.repository;

import ${basePackage}.repository.entity.${model};
import org.springframework.stereotype.Repository;

@Repository
public interface ${model}Repository extends BaseRepository<${model},Long> {
}
